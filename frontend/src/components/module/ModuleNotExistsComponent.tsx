import React from "react";
import { Container } from "reactstrap";

export const ModuleNotExistsComponent = props => {
    return (
        <React.Fragment>
            <Container>
                {/* <h1>Module doesn't exist!</h1> */}
            </Container>
        </React.Fragment>
    );
}